import { styled } from "@mui/material/styles";
import { Box} from "@mui/material";

const MainBoxStyle = styled(Box)(() => ({
  background: "#effcef",
  padding: "4rem",
  borderRadius: "34px",
}));

export { MainBoxStyle };
